# 1.The need of routing
- small, security
- routing is a process to determine the best path to a dst.

# 2.Routing Table
- default route (**not int the routing table**) || (**drop it if both no**)
- **MAC address will be replaced with default gatway** when routing to a diff network
- **IP 255 broadcast > router examine it and not forward**

`arp -a`

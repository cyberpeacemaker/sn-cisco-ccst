# 1. URI
- URI[URL, Fragment]
- **SMTP, [IMAP(modern, multi-device), POP(single, offline, delete after download)]**

**FTP [command and reply / traffic control , file transfer]**
**network protocal** def how msg are exchange between src and dst.
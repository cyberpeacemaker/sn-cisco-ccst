# 1.TCP and UDP
- **TCP header size: 20 bytes (minimum) UDP header size: 8 bytes**
- TCP **reassemble msg**, **window size field provides flow control**

# 2.Port
- Socekt[IP, port]
- IANA, well-know ports
`netstat` 
